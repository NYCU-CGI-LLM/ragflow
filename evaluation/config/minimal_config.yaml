# Minimal configuration example for RAGFlow evaluation
# This is a simple config with only essential parameters

retrieval:
  api_key: "ragflow-c1NzBmZjdhYTM1MDExZjBhMjlhMzdkMj"
  # dataset_id: "6ba750d6a34e11f0a29a37d227f1c0af"
  dataset_name: "simple_rag"
  size: 5  # Number of chunks to retrieve
  dynamic_rerank_limit: true  # Use dynamic rerank limit
  metrics:
    - recall  # Calculate only recall metrics for efficiency
  k_values:
    - 5  # Calculate metrics at K=5

# generation:
#   enabled: true

dataset:
  path: "./dataset/tcm_sd_test_rc_five_updated.json"
  limit: 1  # Limit to 100 samples for quick testing

output:
  results_path: "results/evaluation_results.json"
